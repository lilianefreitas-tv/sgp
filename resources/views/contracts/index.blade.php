<x-app-layout><x-slot name="header"><h1 class="font-semibold">Contratos</h1></x-slot><div class="mx-auto max-w-7xl space-y-6 p-6"><section class="sgp-page-intro flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div><p class="sgp-page-kicker">Contratos e instrumentos</p><h1 class="mt-2 text-2xl font-bold">Compromissos que orientam projetos</h1><p class="mt-2 text-sm leading-6 text-white/80">Registre somente quando houver contrato. Projetos internos continuam simples.</p></div><a href="{{ route('contracts.create') }}" class="sgp-button-secondary shrink-0 sm:w-auto">Novo contrato</a></section>
@forelse($contracts as $contract)<article class="rounded-2xl border border-[#DCE3E7] bg-white p-5 shadow-sm"><div class="flex flex-col gap-3 sm:flex-row sm:justify-between"><div><p class="text-xs text-[#55707B]">{{ $contract->code }} · {{ $contract->status->label() }}</p><h2 class="mt-1 font-bold">{{ $contract->title }}</h2><p class="mt-2 text-sm text-[#667680]">{{ $contract->object }}</p></div><a class="font-semibold text-[#006B88]" href="{{ route('contracts.show',$contract) }}">Abrir →</a></div></article>@empty<div class="rounded-2xl border border-dashed p-10 text-center">Nenhum contrato cadastrado. Isso é normal quando o projeto não exige instrumento contratual.</div>@endforelse
{{ $contracts->links() }}</div></x-app-layout>
