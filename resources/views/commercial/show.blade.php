<x-app-layout><x-slot name="header">Jornada comercial — {{ $initiative->title }}</x-slot>@if(!$opportunity)<form method="POST" action="{{route('commercial.opportunities.store',$initiative)}}">@csrf <input name="title" class="sgp-input" placeholder="Título" required><select name="priority"><option>normal</option><option>high</option><option>low</option></select><button class="sgp-button-primary">Criar oportunidade</button></form>@else <p class="mb-4">Oportunidade {{ $opportunity->code }} — {{ $opportunity->state }}</p><form method="POST" action="{{route('commercial.opportunities.transition',$opportunity)}}">@csrf @method('PATCH')<select name="state">@foreach(['qualified','under_discovery','under_proposal','under_negotiation','won','lost','suspended'] as $s)<option>{{$s}}</option>@endforeach</select><input name="justification" placeholder="Justificativa"><button>Transicionar</button></form><h2>Levantamentos</h2><form method="POST" action="{{route('commercial.assessments.store',$opportunity)}}">@csrf <select name="state"><option>draft</option><option>performed</option><option>validated</option></select><textarea name="needs"></textarea><button>Registrar levantamento</button></form><h2>Propostas</h2><form method="POST" action="{{route('commercial.proposals.store',$opportunity)}}">@csrf <textarea name="scope_summary"></textarea><input name="pricing_model"><button>Criar proposta</button></form><h2>Negociações</h2><form method="POST" action="{{route('commercial.negotiations.store',$opportunity)}}">@csrf <select name="interaction_type"><option>meeting</option><option>email</option><option>acceptance</option></select><input type="datetime-local" name="occurred_at" required><textarea name="summary"></textarea><button>Registrar</button></form>@endif</x-app-layout>
